import path from "path";

export const SORT_STANDARD = ["최신순", "오래된 순", "출처순"];

export const SORT_STANDARD_STATE = ["pubDate", "pubDate", "origin"];

export const JSON_DIRECTORY = path.join(process.cwd(), "model");
